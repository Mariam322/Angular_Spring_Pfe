apiVersion: apps/v1
kind: Deployment
metadata:
  name: compain-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: compain-db
  template:
    metadata:
      labels:
        app: compain-db
    spec:
      containers:
        - name: mariadb
          image: mariadb:10.11
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
            - name: MYSQL_DATABASE
              value: compain_db
          ports:
            - containerPort: 3306
---
apiVersion: v1
kind: Service
metadata:
  name: compain-db-service
spec:
  selector:
    app: compain-db
  ports:
    - port: 3306
      targetPort: 3306
  type: ClusterIP
