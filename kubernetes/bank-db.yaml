apiVersion: apps/v1
kind: Deployment
metadata:
  name: bank-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bank-db
  template:
    metadata:
      labels:
        app: bank-db
    spec:
      containers:
        - name: mariadb
          image: mariadb:10.11
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
            - name: MYSQL_DATABASE
              value: bank_db
          ports:
            - containerPort: 3306
---
apiVersion: v1
kind: Service
metadata:
  name: bank-db-service
spec:
  selector:
    app: bank-db
  ports:
    - port: 3306
      targetPort: 3306
  type: ClusterIP
